# ML Experimentation Platform Frontend

This directory contains the React frontend for the machine learning experimentation web application.

## Setup and Running

1.  **Install dependencies:**
    ```bash
    npm install
    ```

2.  **Run the React development server:**
    ```bash
    npm start
    ```

The application will be accessible at `http://localhost:3000`.
